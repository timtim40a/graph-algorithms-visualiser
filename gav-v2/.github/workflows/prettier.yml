name: Prettier Dry Run

on: push

jobs:
  prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: creyD/prettier_action@v4.6
        with:
          dry: true
